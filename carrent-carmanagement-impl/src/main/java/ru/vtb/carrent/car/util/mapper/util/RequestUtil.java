/*
 * VTB Group. Do not reproduce without permission in writing.
 * Copyright (c) 2017 VTB Group. All rights reserved.
 */

package ru.vtb.carrent.car.util.mapper.util;

import org.apache.commons.codec.binary.Base64;
import org.apache.commons.lang3.StringUtils;

import java.io.IOException;
import java.util.LinkedList;

/**
 * Util class to parse request objects.
 *
 * @author Valiantsin_Charkashy
 */
public class RequestUtil {
//    @SuppressWarnings("unchecked")
//    List<KeyValuePair> getFilterList(String filter) throws IOException {
//        List<KeyValuePair<Object>> filterList = new LinkedList<>();
//        if (StringUtils.isNotBlank(filter)) {
//            KeyValuePair<Object>[] filterParams = Base64.isBase64(filter) ?
//                    JsonUtils.jsonToBean(new String(Base64.decodeBase64(filter), "UTF-8"), KeyValuePair[].class) :
//                    JsonUtils.jsonToBean(filter, KeyValuePair[].class);
//            if (ArrayUtils.isNotEmpty(filterParams)) {
//                filterList.addAll(Arrays.asList(filterParams));
//            }
//        }
//
//        return filterList;
//    }
}
